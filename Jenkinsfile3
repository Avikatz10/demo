pipeline {
    agent any
    stages {
        stage('Create Directory') {
            steps {
                sh 'mkdir new_directory'  // Create directory
                git add 'new_directory'    // Add directory to git
                git commit -m "Added new directory"  // Commit with message
                git push                        // Push changes to GitHub
            }
        }
    }
}
